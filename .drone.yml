common-steps: &common
  image: atlassianlabs/docker-node-jdk-chrome-firefox
  when:
    event: push

pipeline:

  install:
    <<: *common
    commands:
      - npm ci

  execute:
    <<: *common
    image: cypress/included:5.3.0
    group: exe
    commands:
      - npm run test
